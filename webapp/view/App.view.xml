<mvc:View
    controllerName="com.vacaciones.app.controller.App"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:core="sap.ui.core"
    displayBlock="true"
    height="100%">
    <App id="app">
        <pages>
            <Page
                id="mainPage"
                showHeader="true"
                title="{i18n>appTitle}"
                class="sapUiContentPadding">
                <customHeader>
                    <Toolbar>
                        <Button
                            icon="sap-icon://nav-back"
                            press=".onNavBack"
                            visible="{= ${device>/system/phone} }"/>
                        <Title text="{i18n>appTitle}" level="H2"/>
                        <ToolbarSpacer/>
                        <IconTabHeader
                            id="iconTabHeader"
                            selectedKey="{/selectedTab}"
                            select=".onTabSelect"
                            mode="Inline"
                            backgroundDesign="Transparent">
                            <items>
                                <IconTabFilter
                                    key="absences"
                                    text="{i18n>tabAbsences}"
                                    icon="sap-icon://calendar"/>
                                <IconTabFilter
                                    key="calendar"
                                    text="{i18n>tabCalendar}"
                                    icon="sap-icon://calendar"/>
                                <IconTabFilter
                                    key="team"
                                    text="{i18n>tabTeam}"
                                    icon="sap-icon://team"/>
                                <IconTabFilter
                                    key="balance"
                                    text="{i18n>tabBalance}"
                                    icon="sap-icon://balance"/>
                                <IconTabFilter
                                    key="requests"
                                    text="{i18n>tabRequests}"
                                    icon="sap-icon://request"/>
                                <IconTabFilter
                                    key="reports"
                                    text="{i18n>tabReports}"
                                    icon="sap-icon://report"/>
                                <IconTabFilter
                                    key="admin"
                                    text="{i18n>tabAdmin}"
                                    icon="sap-icon://admin"/>
                            </items>
                        </IconTabHeader>
                        <ToolbarSpacer/>
                        <SearchField 
                            width="auto" 
                            search=".onSearch"
                            placeholder="{i18n>searchPlaceholder}"/>
                        <Button 
                            icon="sap-icon://settings" 
                            press=".onSettingsPress"
                            tooltip="{i18n>settingsTooltip}"/>
                        <Button 
                            icon="sap-icon://person-placeholder" 
                            press=".onUserPress"
                            tooltip="{i18n>profileTooltip}"/>
                    </Toolbar>
                </customHeader>
                <content>
                    <NavContainer id="navContainer">
                        <!-- El contenido se cargará dinámicamente según la pestaña seleccionada -->
                    </NavContainer>
                </content>
                <footer>
                    <Toolbar>
                        <ToolbarSpacer/>
                        <ObjectStatus
                            title="{i18n>availableDays}"
                            text="{/counters/available}d"
                            state="Success"
                            icon="sap-icon://calendar"
                            tooltip="{i18n>availableDaysTooltip}"/>
                        <ObjectStatus
                            title="{i18n>totalDays}"
                            text="{/counters/total}d"
                            state="Information"
                            icon="sap-icon://calendar"
                            tooltip="{i18n>totalDaysTooltip}"/>
                        <ObjectStatus
                            title="{i18n>usedDays}"
                            text="{/counters/used}d"
                            state="Warning"
                            icon="sap-icon://calendar"
                            tooltip="{i18n>usedDaysTooltip}"/>
                        <ObjectStatus
                            title="{i18n>pendingDays}"
                            text="{/counters/pending}d"
                            state="Error"
                            icon="sap-icon://calendar"
                            tooltip="{i18n>pendingDaysTooltip}"/>
                        <ToolbarSpacer/>
                        <Button
                            type="Emphasized"
                            text="{i18n>requestAbsence}"
                            icon="sap-icon://add"
                            press=".onRequestAbsence"
                            tooltip="{i18n>requestAbsenceTooltip}"/>
                        <Button
                            icon="sap-icon://refresh"
                            press=".onRefresh"
                            tooltip="{i18n>refreshTooltip}"/>
                        <Button
                            icon="sap-icon://home"
                            press=".onHome"
                            tooltip="{i18n>homeTooltip}"/>
                    </Toolbar>
                </footer>
            </Page>
        </pages>
    </App>
</mvc:View>